# n8n basic auth
N8N_USER=tu_usuario
N8N_PASSWORD=tu_clave

# PostgreSQL
POSTGRES_DB=nombre_db
POSTGRES_USER=usuario
POSTGRES_PASSWORD=clave

# IMAP (correo)
IMAP_USER=tu_email
IMAP_PASSWORD=tu_clave
IMAP_HOST=imap.servidor.com
IMAP_PORT=993

# API externas (OpenAI, etc)
OPENAI_API_KEY=sk-xxxx
